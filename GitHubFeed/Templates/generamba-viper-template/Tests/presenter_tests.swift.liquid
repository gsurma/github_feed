//
//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import XCTest
@testable import {{ module_info.project_targets[0] }}

final class {{ module_info.name }}PresenterTest: XCTestCase {

    fileprivate var routing: Mock{{ module_info.name }}Routing! = Mock{{ module_info.name }}Routing()
    fileprivate var interactor: Mock{{ module_info.name }}Interactor! = Mock{{ module_info.name }}Interactor()
    fileprivate var view: Mock{{ module_info.name }}ViewController! = Mock{{ module_info.name }}ViewController()

    fileprivate var presenter: {{ module_info.name }}Presenter<MockInteractor, MockRouting, MockViewController>!

    override func setUp() {
        super.setUp()

        presenter = {{ module_info.name }}Presenter(routing: routing, interactor: interactor)
        presenter.attach(viperView: view)
    }

    override func tearDown() {
        super.tearDown()
    }
}
